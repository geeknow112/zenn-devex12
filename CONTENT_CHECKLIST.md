# 記事品質チェックリスト（Zenn）

PRを作成する前に、この項目をすべて満たしていることを確認する。
満たしていない場合はPRを作らず、下書きの状態で修正を続ける。

## 1. プレースホルダー残存チェック（必須）

本文全体に以下の文字列が1つも残っていないこと。

- `[ここに`
- `[要記入`
- `TODO`
- `TBD`
- `XXX`
- `記入]`

確認コマンド（記事ファイルに対して実行し、何も出力されないこと）:

```bash
grep -nE '\[ここに|\[要記入|TODO|TBD|XXX|記入\]' articles/<記事ファイル名>.md
```

プレースホルダーが残っている＝実測していない数値がある、ということ。
「参考値」「体感」などの言葉でごまかして空欄のまま出すのは不可。
実測できないなら、その記述・その段落ごと削除する。

## 2. CTAは記事末尾1箇所のみ（確定運用ルール）

- CTA（Udemy講座・関連記事への誘導・フォロー導線など）は本文中に**1箇所だけ**。
- 記事の途中や複数箇所にCTAを挟まない。
- CTAを入れる場合は記事の最後（参考リンクの前後）にまとめる。
- CTAなし（純粋な技術記事として完結させる）も可。ゼロは問題ないが、複数は不可。

## 3. 数値は実測値のみ

- 本文に載せる割合・件数・時間・金額は、自分が実際に計測・実行して得た値であること。
- 計測していない数値を「推定」「目安」として書かない。
- 計測できなかった場合は、その数値を使った文・表の行ごと削除する。
  （プレースホルダーとして残すのも不可。書くなら実測、書けないなら削除、の二択）
- 出典が公開情報（公式ドキュメント等）の数値は、実測値と混同しないよう明記する。

## 4. frontmatterの確認

- `title` が記事の内容と一致し、誇張・釣りタイトルになっていないこと
- `emoji`, `type`, `topics` が正しく設定されていること
- `published` の値が公開フローと矛盾しないこと（下書き段階では `false`）

## 5. 品質基準（大前提）

- 一般論だけの薄い記事にしない
- 独自の実体験・実際に踏んだ失敗を最低1つ入れる
- 具体的な数値（実測に限る）を入れる
- 代替手段との比較を入れる
- Q&Aセクションを入れる
- TL;DRと手順の羅列だけで終わらせない

---

このチェックリストを満たさないままPRを作った結果、2026-09-06に16本のPRを
プレースホルダー残存・品質基準未達で一括クローズする事態が発生した。
再発防止のため、PR作成前に必ずこのファイルを確認すること。
